---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: ovhmachinepools.infrastructure.cluster.x-k8s.io
  labels:
    cluster.x-k8s.io/provider: infrastructure-ovhcloud
    cluster.x-k8s.io/v1beta1: v1alpha2
spec:
  group: infrastructure.cluster.x-k8s.io
  names:
    kind: OVHMachinePool
    listKind: OVHMachinePoolList
    plural: ovhmachinepools
    singular: ovhmachinepool
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Pool is ready
      jsonPath: .status.ready
      name: Ready
      type: string
    - description: Current replicas
      jsonPath: .status.replicas
      name: Replicas
      type: integer
    name: v1alpha2
    schema:
      openAPIV3Schema:
        description: OVHMachinePool is the Schema for the ovhmachinepools API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OVHMachinePoolSpec defines the desired state of OVHMachinePool.
            properties:
              flavorName:
                description: FlavorName is the OVH instance flavor (e.g. "b2-7", "b2-15").
                type: string
              imageName:
                description: ImageName is the OS image name.
                type: string
              providerIDList:
                description: |-
                  ProviderIDList is set by the controller with the provider IDs of all
                  instances in the pool.
                items:
                  type: string
                type: array
              rootDiskSize:
                description: RootDiskSize is the root disk size in GB. 0 uses the
                  flavor default.
                type: integer
              sshKeyName:
                description: SSHKeyName is the SSH key to inject.
                type: string
            required:
            - flavorName
            - imageName
            type: object
          status:
            description: OVHMachinePoolStatus defines the observed state of OVHMachinePool.
            properties:
              conditions:
                description: Conditions defines current service state of the pool.
                items:
                  description: Condition defines an observation of a Cluster API resource
                    operational state.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed. If that is not known, then using the time when
                        the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This field may be empty.
                      maxLength: 10240
                      minLength: 1
                      type: string
                    reason:
                      description: |-
                        reason is the reason for the condition's last transition in CamelCase.
                        The specific API may choose whether or not this field is considered a guaranteed API.
                        This field may be empty.
                      maxLength: 256
                      minLength: 1
                      type: string
                    severity:
                      description: |-
                        severity provides an explicit classification of Reason code, so the users or machines can immediately
                        understand the current situation and act accordingly.
                        The Severity field MUST be set only when Status=False.
                      maxLength: 32
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions
                        can be useful (see .node.status.conditions), the ability to deconflict is important.
                      maxLength: 256
                      minLength: 1
                      type: string
                  required:
                  - lastTransitionTime
                  - status
                  - type
                  type: object
                type: array
              failureMessage:
                description: FailureMessage is a full error message.
                type: string
              failureReason:
                description: FailureReason is the short name for a failure.
                type: string
              infrastructureReady:
                description: InfrastructureReady is true when the pool infrastructure
                  is ready.
                type: boolean
              instanceIDs:
                description: InstanceIDs tracks the OVH instance UUIDs in the pool.
                items:
                  type: string
                type: array
              ready:
                description: Ready is true when all pool instances are provisioned.
                type: boolean
              readyReplicas:
                description: ReadyReplicas is the number of ready instances.
                format: int32
                type: integer
              replicas:
                description: Replicas is the current number of instances in the pool.
                format: int32
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
